import { component$ } from '@builder.io/qwik';
import ContentDoc from '~/components/content-doc/content-doc';

export default component$(() => {

  return (
    <ContentDoc fileName="PrivacyPolicy" />
  );
});