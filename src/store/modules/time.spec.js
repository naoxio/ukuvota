// import * as time from './time'

describe('timer.buildOutput', () => {
  it('should format the timer correctly', () => {
    // no expectations
    // expect(buildOutput(1, 0, 0, 1)).toEqual('1 day')
    // expect(buildOutput(0, 20, 0, 0)).toEqual('20 hours')
    // expect(buildOutput(0, 0, 5, 1)).toEqual('5 minutes 1 second')
  })
})
