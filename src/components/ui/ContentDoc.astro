---
interface Props {
    file_name: string
}
const { file_name } = Astro.props;

import { astroI18n } from "astro-i18n";

const content = await import(`../../content/${astroI18n.locale}/${file_name}.md`);
---

<Fragment set:html={content.compiledContent()} />
