---
interface Props {
    title: string,
}
const { title} = Astro.props;
---
<div class="flex flex-col justify-center items-center w-60 m-2 text-center">
    { title && <h3>{ title }</h3> }
    <div class="flex justify-center items-center">
        <slot/>
    </div>
</div>
